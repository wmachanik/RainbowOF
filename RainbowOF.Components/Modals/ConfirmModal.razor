@using Blazorise; 

<Modal @ref="modalRef">
    <ModalContent IsCentered="true" Size="ModalSize.Large">
        <ModalHeader>
            <ModalTitle>@_ConfirmationTitle</ModalTitle>
            <CloseButton Clicked="@(() => OnConfirmationChangeAsync(false))" />
        </ModalHeader>
        <ModalBody>
            @_ConfirmationMessage
        </ModalBody>
        <ModalFooter>
            <Blazorise.Button Color="@_ConfirmColor" Clicked="@(() => OnConfirmationChangeAsync(true))">@_ConfirmButtonText</Blazorise.Button>
            <Button Color="Color.Secondary" Clicked="@(() => OnConfirmationChangeAsync(false))">@_CancelButtonText</Button>
        </ModalFooter>
    </ModalContent>
</Modal>
